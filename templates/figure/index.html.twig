{% extends 'base.html.twig' %}

{% block title %}Figure index{% endblock %}

{% block body %}
    <h1>Figure index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>Date</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for figure in figures %}
            <tr>
                <td>{{ figure.id }}</td>
                <td>{{ figure.name }}</td>
                <td>{{ figure.description }}</td>
                <td>{{ figure.creationDate ? figure.creationDate|date('d-m-Y') : '' }}</td>
                <td>
                    <a href="{{ path('app_figure_show', {'id': figure.id, 'slug': figure.slug}) }}">voir</a>
                    <a href="{{ path('app_figure_edit', {'id': figure.id}) }}">éditer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">Vous n'avez pas ajouter de figure.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_figure_new') }}">Ajouter une figure</a>
{% endblock %}
